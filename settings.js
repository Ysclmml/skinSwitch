/** dynamicWorker.js ***/  // 暂时先尝试加个配置文件, 以后可能会用到.
dwStopTimes = 10  // stop函数停止次数

dwDefaultDaiJiAction = ['DaiJi', 'play']  // 默认的待机标签, 忽略大小写

dwBeiJingDaiJiActions = ['DaiJi', 'BeiJing', 'play']  // 背景的待机标签. 版本比较多

